settings.outformat="pdf";
settings.prc=false;
size(5cm,0);
import three;

draw(unitsphere);


// settings.prc=false;，设置三维图形不可旋转。只有pdfviewer为adobe，并且Asymptote不是嵌入在TeX文件中时，可配置为true

// import three;，导入three模块。三维图形相关内容包含在three模块中

// unitsphere为三维单位球
