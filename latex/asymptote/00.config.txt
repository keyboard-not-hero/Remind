import settings;
outformat="pdf";
pdfviewer="evince";


// '//'为注释符

// ';'为每条语句结束标识符

// import settings; 导入settings包

// outformat; 设置输出格式. 默认为PostScript文件(.eps后缀)

// pdfviewer; 设置pdf格式预览器

// config文件搜索路径, 按优先级序排列:
// 1.当前工程目录
// 2.环境变量ASYMPTOTE_DIR的值或配置文件中dir指令(UNIX使用:分隔目录, Windows使用;分隔目录)
// 3.环境变量ASYMPTOTE_HOME的值或~/.asy目录
// 4.system级目录(UNIX - /usr/local/share/asymptote, Windows - C:\Program Files\Asymptote)
// 5.example目录(UNIX - /usr/local/share/doc/asymptote/examples, Windows - C:\Program Files\Asymptote\examples)
