1.持久连接
HTTP/0.9及部分HTTP/1.0在单次TCP三次握手中，只能发送一次请求/响应

部分HTTP/1.0及HTTP/1.1实现持久连接，即keep-alive，能保证在任意一端没有提出断开连接，则保持TCP连接，并且可以再次期间多次发送请求/响应

2.管线化
由于持久连接特性，单次TCP连接可以多次发送请求/响应，因此多个请求或多个响应可以并发进行，不需要等待前一个请求/响应pair结束
